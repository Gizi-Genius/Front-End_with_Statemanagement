import 'package:flutter/material.dart';

class MenuFoodUser extends StatelessWidget {
  const MenuFoodUser({super.key});

  @override
  Widget build(BuildContext context) => SafeArea(child: Scaffold());
}
